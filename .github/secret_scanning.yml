# GitHub secret scanning configuration for security-audit
#
# The tests/fixtures directory intentionally contains fake secrets in known
# placeholder formats (AWS example keys, documented non-production tokens) to
# test pattern detection. These are not real credentials.
#
# See: https://docs.github.com/en/code-security/secret-scanning/push-protection

paths-ignore:
  - "tests/fixtures/**"
